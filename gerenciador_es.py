class GerenciadorIO:
    def __init__(self) -> None:
        self.IOList = []   
        # system call
        #Enum para a sys class

        #susstime  #Fazer interface para todoos ios, eles vão ter suporte a interrupção, leitura
        #Mapear drive com o nome
        #Erros uniformes
        #PRoteção de ados ao acesso
        #Buffer
        #Cache

        #Fila de requisições para cada dispositivo, em seguida definiri a prioriadea

        #reentrantes, o que significa que um driver deve
        #supor que ele pode ser chamado uma segunda vez antes que a
        #primeira chamada tenha sido concluída.
        #O hardware gera TRAP pra avisar o termino de uma operação
        #Rotina d etratamento de interrupção vetor de interrução
        #O processo que soliciout ES ficabloqueado ate receber a repsota
        #pag 35 nos slides

    def gravarDadosEmRegistradores(self):
    def lerDadosDosRegistradores(self):
    